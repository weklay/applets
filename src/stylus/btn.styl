//按钮
@import "common.styl"

.btn-box {
  margin: 15px !important;
}

.btn {
  position: relative;
  background-color: #fff;
  box-sizing: border-box;
  text-decoration: none;
  margin-bottom: 10px;
  text-align: center;
  border-radius: 2px;
  line-height: 45px;
  font-size: 16px;
  padding: 0 15px;
  color: #333;
  height 45px;

  &::after {
    border_line();
    border-width: 1px;
    border-radius: 4px;
  }
}

/* type */
.btn-c_primary {
  color: #fff;
  background-color: #4b0;

  &::after {
    border-color: #0a0;
  }
}

.btn-c_warn {
  color: #fff;
  background-color: #f85;

  &::after {
    border-color: #f85;
  }
}

.btn-c_danger {
  color: #fff;
  background-color: #f44;

  &::after {
    border-color: #e33;
  }
}

.btn-c_DodgerBlue {
  color: #fff;
  background-color: DodgerBlue;

  &::after {
    border-color: DodgerBlue;
  }
}

.btn-c_diyBlue {
  color: #fff;
  background-color: #25ABDF;

  &::after {
    border-color: #25ABDF;
  }
}

/* size */
.btn-s_large {
  border-radius: 0;
  margin-bottom: 0;
  line-height: 50px;
  border: none;
  height: 50px;
}

.btn-s_small {
  display: inline-block;
  line-height: 30px;
  margin-right: 5px;
  margin-bottom: 0;
  font-size: 12px;
  height: 30px;
}

.btn-s_mini {
  display: inline-block;
  padding-right: 5px;
  padding-left: 5px;
  line-height: 21px;
  margin-right: 5px;
  margin-bottom: 0;
  font-size: 10px;
  height: 22px;
}

/* 背景透明 */
.btn-plain.btn {
  background-color: transparent;
}

.btn-plain.btn-c_primary {
  color: #06BF04;
}

.btn-plain.btn-c_warn {
  color: #FF6600;
}

.btn-plain.btn-c_danger {
  color: #FF4444;
}

/* 重写button组件的button-hover样式 */
.button-hover {
  opacity: 0.9;
}

/* disabled */
.btn.btn-disabled {
  /* 防止样式被 button[disabled] 的规则覆盖，所以使用了important */
  color: #fff !important;
  background: #999 !important;
  border-color: #999 !important;
  cursor: not-allowed !important;
  opacity: 1 !important;

  &::after {
    border-color: #999 !important;
  }
}
